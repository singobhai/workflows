name: Scheduled Test every 5 min

on:
  schedule:
      - cron: '*/5 * * * *'  # Runs at 7:30 PM UTC every day

jobs:
   build:
    runs-on: ubuntu-latest
    steps:
      - name: Print Current Date and Time
        run: date -u
