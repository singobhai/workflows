name: Scheduled Test

on:
  schedule:
    - cron: '*/3 * * * *'  # Runs at 7:30 PM UTC every day

jobs:
   build:
    runs-on: ubuntu-latest
    steps:
      - name: Print Current Date and Time
        run: date -u
