name: Scheduled Test

on:
  schedule:
    - cron: '43 19 * * *'  # Runs at 7:30 PM UTC every day

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Current Date and Time
        run: date -u
